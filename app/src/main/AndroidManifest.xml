<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kasia.mobilefamily">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".ListFamilyMembersActivity" />
        <activity android:name=".FamilyTreeActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".MessagesActivity" />
        <activity android:name=".EventsListActivity" />
        <activity android:name=".EventPhotosActivity" />
        <activity android:name=".MemberDetailsActivity" />
        <activity android:name=".AddFamilyMemberActivity" />
        <activity android:name=".MessageContentActivity"/>
        <activity android:name=".AddEventActivity"/>
        <activity android:name=".AddRelativeActivity"/>
        <activity android:name=".EventDetailsActivity">
        </activity>
    </application>

</manifest>